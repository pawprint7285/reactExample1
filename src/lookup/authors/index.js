import { authorsReducer } from "./reducers";
export * from "./actions";

// the authors are used for the drop down values, and in the project
// usually, such drop down values are called lookup value and loaded
// in advance

export default authorsReducer;
