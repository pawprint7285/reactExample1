import React from "react";

const About = () => (
  <div>
    <h2>About</h2>
    <p>
      This app uses React, Redux, React Router, and many other helpful
      libraries.
    </p>
  </div>
);

// do nothig just push for develop build
// and when it successes
// will remove the unwanted directory
export default About;
